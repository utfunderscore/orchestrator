meta {
  name: create template
  type: http
  seq: 1
}

post {
  url: http://localhost:9191/docker/templates/
  body: json
  auth: none
}

body:json {
  {"id":"dev-client","dockerImage":"orchestrator-dev-client:latest", "network":"orchestrator"}
}
